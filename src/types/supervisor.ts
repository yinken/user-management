export type Supervisor = {
  agentId: number;
  avatarUrl: string;
  alias: string;
  lastUpdated: number;
  id: string;
};

export type MappedSupervisor = {
  agentId: number;
  avatarUrl: string;
  title: string;
  id: string;
};
